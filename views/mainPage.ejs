<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Co-op Whiteboard</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="css/mainPage.css">
    <script scr="/js/env/env.js"></script>

</head>

<body>
    <header class="d-flex justify-content-between align-items-center">
        <div class="d-flex mx-sm-2 p-1 align-items-center">
            <img src=<%= user.avtURL %>  class="me-2" id="avatar">
            Welcome,&ThickSpace;<span class="user-name"><b><%= user.name %></b></span> </span>
        </div>
        <div class="mx-sm-2 p-1">
            <a class="btn btn-sign-out" href="/auth/logout">
                <i class="bi bi-box-arrow-right"></i>
                Logout
            </a>
        </div>
    </header>
    <main>
        <div class="container-fluid vertical-center">
            <div class="row justify-content-center">
                <div class="text-center">
                    <h1>Co-op Whiteboard</h1>
                </div>
            </div>

            <div class="row justify-content-center mt-2">
                <button class="btn btn-block btn-join" id="btn-join" onclick="handleBtnEvent('btn-join-click')" hidden>
                    <a href="#"></a>
                    <div class="text">
                        Join a whiteboard
                    </div>
                </button>
                <button class="btn btn-block btn-create" id="btn-create" onclick="handleBtnEvent('btn-create-click')" hidden>
                    <a href="#"></a>
                    <div class="text">
                        Create a whiteboard
                    </div>
                </button>
            </div>

            <div class="row justify-content-center">
                <div id="join" class="text-center" hidden>
                    <label id="joinText">Join a whiteboard</label>
                    <input type="text" class="form form-control text-center" id="enterCode"
                        placeholder="Enter whiteboard code" hidden='true'>
                </div>

                <div id="create" class="text-center justify-content-center" hidden>
                    <label id="createText">Create a whiteboard</label>
                    <div class="justify-content-center text-start" id="instruction">
                        <ul>
                            <li>
                                Whiteboard name cannot contain spaces.
                            </li>
                            <li>
                                Whiteboard name must contain only letters, numbers, spaces.
                            </li>
                            <li>
                                Whiteboard name must be between 2-20 characters long.
                            </li>
                        </ul>
                    </div>
                    <input type="text" class="form form-control text-center" id="enterName"
                        placeholder="Enter whiteboard name" hidden='true' minlength="2" maxlength="20" autocomplete="off">
                </div>
            </div>

            <div id="message" hidden></div>

            <div class="row justify-content-center mt-2">
                <button class="btn btn-block-1 btn-secondary" id="back" onclick="handleBtnEvent('init')" hidden>
                    <a href="#"></a>
                    <div class="text-1">Back</div>
                </button>
                <button class="btn btn-block-1 btn-primary" id="submit" hidden>
                    <a href="#"></a>
                    <div class="text-1" id="submitText"></div>
                </button>
            </div>

            <table class="table mt-2" id="recentBoard" hidden>
                <thead>
                    <tr>
                        <th class="text-center" colspan="4">
                            Your recent whiteboards
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <% if (user.boards.length === 0) { %>
                        <tr>
                            <td colspan="3" class="text-center">You don't have any recent whiteboards</td>
                        </tr>
                    <% } else { %>
                        <tr>
                            <th scope="col">Name</th>
                            <th scope="col">ID</th>
                            <th scope="col"></th>
                        </tr>
                        
                        <% recentBoards.forEach(board => { %>
                            <tr>
                                <td scope="col"><%= board.name%></td>
                                <td scope="col"><%= board._id%></td>
                                <td>
                                    <a href=<%= '/board/' + board._id %>  >Join</a>
                                </td>
                            </tr>
                        <% }) %> 
                    <% } %>
                
                </tbody>
            </table>

        </div>
    </main>
    <footer class="text-center">
        Developed by NetScape Team
    </footer>

    <script src="/js/joinWhiteboard.js"></script>
    <script src="/js/createWhiteboard.js"></script>
    <script src="/js/mainPage.js"></script>

</body>

</html>